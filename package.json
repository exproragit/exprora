{
  "name": "exprora",
  "version": "1.0.0",
  "description": "Complete A/B Testing and Conversion Optimization Platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "sdk"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend && npm run build:sdk",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:sdk": "cd sdk && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../sdk && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

